#ifndef HDR_H
#define HDR_H

/**
 * @brief Header refresh, called when needed
 * @note Currently it's only called every second
 * to handle the clock. Exception counter is refreshed
 * every second.
 */
void update_header();

#endif
